# Configuración para desarrollo con PostgreSQL local
spring.datasource.url=jdbc:postgresql://localhost:5432/gestions_db
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=u5u4r10219*

# JPA Configuration para desarrollo
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Actuator endpoints
management.endpoints.web.exposure.include=health,info

# Logging para desarrollo
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT secret para desarrollo
jwt.secret=devsecret-devsecret-devsecret-dev
